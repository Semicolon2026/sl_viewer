name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build container
        run: docker build -t sl-viewer .

      - name: Run container
        run: docker run --rm sl-viewer
